apply from: "$rootDir/buildSrc/module/base-android.gradle"

dependencies {
    // Internal modules
    implementation project(path: ':features:ranges:domain')
    implementation project(path: ':features:ranges-practice:domain')
}

android {
    namespace 'com.mctech.pokergrinder.ranges_practice.presentation.navigation'
}