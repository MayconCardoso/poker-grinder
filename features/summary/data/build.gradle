apply from: "$rootDir/buildSrc/module/feature/data.gradle"

dependencies {
    // Internal Modules.
    implementation project(path: ':features:grind:data')
    implementation project(path: ':features:summary:domain')
}

android {
    namespace 'com.mctech.pokergrinder.summary.data'
}