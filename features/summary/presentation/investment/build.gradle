apply from: "$rootDir/buildSrc/module/feature/presentation.gradle"

dependencies {
    // Internal modules
    implementation project(path: ':features:summary:domain')
    implementation project(path: ':features:settings:domain')

    // Ui
    implementation libraries.androidCardView
}

android {
    namespace 'com.mctech.pokergrinder.summary.presentation.investment'
}