apply from: "$rootDir/buildSrc/module/base-android.gradle"

dependencies {
    // Internal modules
    implementation project(path: ':features:summary:domain')
}

android {
    namespace 'com.mctech.pokergrinder.summary.presentation.navigation'
}