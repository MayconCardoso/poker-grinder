apply from: "$rootDir/buildSrc/module/base-android.gradle"

dependencies {
    // Internal modules
    implementation project(path: ':features:tournaments:domain')
}

android {
    namespace 'com.mctech.pokergrinder.tournament.presentation.navigation'
}