apply from: "$rootDir/buildSrc/module/feature/presentation.gradle"

dependencies {
    // Internal modules
    implementation project(path: ':features:deck:domain')

    // Ui
    implementation libraries.androidCardView
}

android {
    namespace 'com.mctech.pokergrinder.deck.presentation.card_picker'
}