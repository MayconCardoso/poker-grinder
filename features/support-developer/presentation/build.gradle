apply from: "$rootDir/buildSrc/module/feature/compose.gradle"

dependencies {
    // Internal modules
    implementation project(path: ':features:support-developer:domain')
}

android {
    namespace 'com.mctech.pokergrinder.support_developer.presentation'
}