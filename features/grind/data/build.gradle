apply from: "$rootDir/buildSrc/module/feature/data.gradle"

dependencies {
    // Internal Modules.
    implementation project(path: ':features:grind:domain')
    implementation project(path: ':features:grind-tournament:data')

    // Internal test Modules
    testImplementation project(path: ':features:grind:testing')
}

android {
    namespace 'com.mctech.pokergrinder.grind.data'
}