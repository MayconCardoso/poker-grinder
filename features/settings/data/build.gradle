apply from: "$rootDir/buildSrc/module/feature/data.gradle"

dependencies {
    // Internal Modules.
    implementation project(path: ':features:settings:domain')
}

android {
    namespace 'com.mctech.pokergrinder.settings.data'
}