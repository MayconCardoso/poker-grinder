apply from: "$rootDir/buildSrc/module/feature/data.gradle"

dependencies {
    // Internal Modules.
    implementation project(path: ':features:ranges:domain')
}

android {
    namespace 'com.mctech.pokergrinder.ranges.data'
}