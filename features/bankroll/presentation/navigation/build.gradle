apply from: "$rootDir/buildSrc/module/base-android.gradle"

dependencies {
    // Internal modules
    implementation project(path: ':features:bankroll:domain')
}

android {
    namespace 'com.mctech.pokergrinder.bankroll.presentation.navigation'
}