apply from: "$rootDir/buildSrc/module/feature/data.gradle"

dependencies {
    // Internal Modules.
    implementation project(path: ':features:bankroll:domain')
    testImplementation project(path: ':features:bankroll:testing')
}

android {
    namespace 'com.mctech.pokergrinder.bankroll.data'
}