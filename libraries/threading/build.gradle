apply plugin: 'kotlin'

dependencies {
    implementation libraries.kotlinCoroutineCore
    implementation libraries.kotlinCoroutineAndroid

    // dagger
    implementation libraries.googleDagger

    // Testing
    testImplementation project(path: ':testing:common-test')
}
